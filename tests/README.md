# Tests

This folder is reserved for simple consistency checks and optional tests.

In this project, no automated tests are required.
Basic validation is performed directly in the data processing scripts
(e.g. column checks and value ranges).




